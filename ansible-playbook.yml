---
- name: Deploy Flask App to Azure Container Instances
  hosts: localhost
  tasks:
    - name: Docker YUM
      yum:
        name: 
          - docker
          - python3-pip
        state: present

    - name: Start Docker service
      service:
        name: docker
        state: started
        enabled: yes

    - name: Pull Docker image for Flask application
      command: "docker pull 3345blog.azurecr.io/blogv2:{{ github.sha }}"
      register: pull_output
      environment:
        REGISTRY_USERNAME: "{{ secrets.REGISTRY_USERNAME }}"
        REGISTRY_PASSWORD: "{{ secrets.REGISTRY_PASSWORD }}"

    - name: Create Flask Docker container
      community.docker.docker_container:
        name: blogaci
        image: "3345blog.azurecr.io/blogv2:{{ github.sha }}"
        state: started
        ports:
          - "80:80"
        restart_policy: never
      environment:
        MONGO_URI: "mongodb://blogdbaccount:DJeiV2yZlFvd2EpoLQNNYCdEwmrgLsy8nkirggYZGHlw2fXSHpCXP7GnijKugivxR24ZW4uhlvTwACDbgxdwUQ==@blogdbaccount.mongo.cosmos.azure.com:10255/?ssl=true&retrywrites=false&replicaSet=globaldb&maxIdleTimeMS=120000&appName=@blogdbaccount@"
      login_username: "{{ secrets.REGISTRY_USERNAME }}"
      login_password: "{{ secrets.REGISTRY_PASSWORD }}"
